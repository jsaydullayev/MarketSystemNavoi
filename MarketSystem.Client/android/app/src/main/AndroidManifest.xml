<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Image Picker permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 10 va undan past versiyalar uchun -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Android 13+ (API 33+) granular media ruxsatlari — QASDDAN OLIB TASHLANGAN.
         Google Play "Photo and video permissions" siyosati: galereyadan rasm
         tanlash uchun tizim Photo Picker'i ishlatilishi SHART; keng qamrovli
         READ_MEDIA_* ruxsatlari faqat asosiy funksiya butun galereyaga kirishni
         talab qilsagina asoslanadi. Bu ilovada galereya faqat BITTA rasm tanlash
         uchun ochiladi (mahsulot / profil rasmi) — buni Android Photo Picker
         (ActivityResultContracts.PickVisualMedia; main.dart'da yoqilgan) HECH
         QANDAY ruxsatsiz bajaradi. Shuning uchun uchala READ_MEDIA_* ruxsatini
         ham merged manifestdan aniq OLIB TASHLAYMIZ — image_picker_android
         plagini yoki boshqa transitiv kutubxona ularni merge qilsa ham. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />

    <application
        android:label="Strotech"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
              
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter tomonidan ishlatiladi -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Matnni qayta ishlash uchun kerak bo'lishi mumkin -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
